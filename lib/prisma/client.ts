/**
 * Single import surface for the Prisma 7 generated client.
 *
 * The generator output is configured in prisma/schema.prisma:
 *   output = "../generated/prisma"
 *
 * That directory is gitignored and produced by `prisma generate`, which runs
 * automatically via the `postinstall` and `prebuild` npm scripts. Importing
 * through this module means the generated path is referenced in exactly one
 * place instead of across the whole codebase.
 */
export * from "@/generated/prisma/client";
